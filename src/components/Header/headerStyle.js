import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  Header: {
    left: 0,
    top: 0,
    backgroundColor: "#a2b6e4",
  },

  headerText: {
    textAlign: "center",
    flex: 1,
  },
  image: {
    resizeMode: "contain",
    position: "absolute",
    right: 0,
    bottom: 0,
  },
});

export default styles;
